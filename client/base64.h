#ifndef __BASE64_H
#define __BASE64_H

char*    b64encode(const char *);                  // Encodes a string to Base64.
char*    b64decode(const char *);                  // Decodes a string to ASCII.

#endif
